
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/service_auth
    username: authtest
    password: authtest123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
server:
  port: 8081
